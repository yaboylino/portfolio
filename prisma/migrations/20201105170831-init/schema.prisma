generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = "***"
}

model Portfolio {
  id    Int    @id @default(autoincrement())
  name  String
  url   String
  photo String
}
