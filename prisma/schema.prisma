generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Portfolio {
  id    Int    @id @default(autoincrement())
  name  String
  url   String
  photo String
}
